#include <stdio.h>

int main() {
    // Initializing an array
    int arr[5] = {1, 2, 3, 4, 5};

    // Displaying array elements
    printf("Array elements: ");
    for (int i = 0; i < 5; i++) {
        printf("%d ", arr[i]);
    }

    return 0;
}
